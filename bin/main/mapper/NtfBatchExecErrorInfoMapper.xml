<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.toyota.tsc.notificationhub.repositories.NtfBatchExecErrorInfoMapper">
  <insert id="insert" parameterType="com.toyota.tsc.notificationhub.repositories.NtfBatchExecErrorInfoEntity" timeout="5">
    INSERT INTO ntf_batch_exec_error_info (
      registraion_serial_number,
      sequence_number,
      internal_user_id,
      notification_status,
      push_status,
      primary_contact_status,
      errored_at,
      created_at,
      updated_at
    ) VALUES (
      #{entity.registrationSerialNumber},
      #{entity.sequenceNumber},
      #{entity.internalUserId},
      #{entity.notificationStatus},
      #{entity.pushStatus},
      #{entity.primaryContactStatus},
      CURRENT_TIMESTAMP,
      CURRENT_TIMESTAMP,
      CURRENT_TIMESTAMP
    );
  </insert>
</mapper>